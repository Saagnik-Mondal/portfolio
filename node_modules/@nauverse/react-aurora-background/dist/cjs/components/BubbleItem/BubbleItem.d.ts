import React from 'react';
import { Color } from '../../types';
/**
 * It's a React component that renders a div with a background color and some animation properties.
 * @param  - index - the index of the bubble item
 */
export declare function BubbleItem({ index, color, animDuration, useRandomness, blurAmount }: {
    blurAmount: number | string;
    index: number;
    color: Color;
    animDuration: number;
    useRandomness: boolean;
}): React.JSX.Element;
