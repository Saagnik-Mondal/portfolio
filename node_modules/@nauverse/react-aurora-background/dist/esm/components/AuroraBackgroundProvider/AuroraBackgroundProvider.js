import React from 'react';
import { AuroraBackground } from '../AuroraBackground/AuroraBackground';
import { ChildrenContainer } from '../ChildrenContainer';
import styles from './AuroraBackgroundProvider.module.css';
/**
 * AuroraBackgroundProvider is a function that returns a div with a className of auroraHolder. Inside
 * the div is a component called AuroraBackground and a component called ChildrenContainer
 * @param {IAuroraBackgroundProvider}  - IAuroraBackgroundProvider
 * @returns A div with a className of auroraHolder.
 */
export function AuroraBackgroundProvider({ children, colors, numBubbles, animDuration, blurAmount, bgColor, useRandomness, style, className }) {
    return (React.createElement("div", { className: `${styles.auroraHolder}${className !== null && className !== void 0 ? className : ''}`, style: style },
        React.createElement(AuroraBackground, { useRandomness: useRandomness, colors: colors, numBubbles: numBubbles, animDuration: animDuration, blurAmount: blurAmount, bgColor: bgColor }),
        React.createElement(ChildrenContainer, null, children)));
}
