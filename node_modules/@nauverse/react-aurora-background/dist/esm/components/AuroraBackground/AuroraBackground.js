import React, { useCallback, useMemo } from "react";
import { COLOR_DODGER_BLUE_2 } from "../../lib/colors";
import { DEFAULT_CONFIG } from "../../lib/constants";
import { BubbleItem } from "../BubbleItem";
import styles from './AuroraBackground.module.css';
/**
 * It creates a bunch of bubbles, each with a different color, and animates them
 * @param {AuroraBackgroundConfig}  - bgColor - the background color of the gradient
 * @returns A React component.
 */
export function AuroraBackground({ bgColor, colors, numBubbles, animDuration, blurAmount, useRandomness }) {
    const _bgColor = useMemo(() => bgColor !== null && bgColor !== void 0 ? bgColor : DEFAULT_CONFIG.bgColor, [bgColor]);
    const _animDuration = useMemo(() => animDuration !== null && animDuration !== void 0 ? animDuration : DEFAULT_CONFIG.animDuration, [animDuration]);
    const _blurAmount = useMemo(() => blurAmount !== null && blurAmount !== void 0 ? blurAmount : DEFAULT_CONFIG.blurAmount, [blurAmount]);
    const _useRandomness = useMemo(() => useRandomness !== null && useRandomness !== void 0 ? useRandomness : DEFAULT_CONFIG.useRandomness, [useRandomness]);
    /* A function that returns a color based on the index. */
    const _getColorByIndex = useCallback((index) => {
        var _a;
        const _arrColors = colors !== null && colors !== void 0 ? colors : DEFAULT_CONFIG.colors;
        let _fixedIndex = index;
        if (index >= _arrColors.length)
            _fixedIndex = Math.floor(index % _arrColors.length);
        if (index <= -_arrColors.length)
            _fixedIndex = -1 * Math.floor(index % _arrColors.length);
        if (index <= 0 && index > -_arrColors.length)
            _fixedIndex = -1 * index;
        return (_a = _arrColors[_fixedIndex]) !== null && _a !== void 0 ? _a : COLOR_DODGER_BLUE_2;
    }, [colors]);
    /* Creating an array of BubbleItem components. */
    const bubbleItems = useMemo(() => {
        const _amountBubbles = numBubbles !== null && numBubbles !== void 0 ? numBubbles : DEFAULT_CONFIG.numBubbles;
        return new Array(_amountBubbles).fill(0).map((_v, index) => {
            const _bubbleColor = _getColorByIndex(index);
            return React.createElement(BubbleItem, { blurAmount: _blurAmount, key: `aurora-bubble-item-${index}`, index: index, color: _bubbleColor, animDuration: _animDuration, useRandomness: _useRandomness });
        });
    }, [numBubbles]);
    return (React.createElement("div", { className: styles.container },
        React.createElement("div", { className: styles.gradientWrapper, style: {
                background: _bgColor
            } }, bubbleItems)));
}
